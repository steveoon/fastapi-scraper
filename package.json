{
  "name": "fastapi-scraper",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "fastapi-dev": "eval \"$(conda shell.bash hook)\" && conda activate scrapeai && pip install -r requirements.txt && python -m uvicorn api.index:app --reload"
  }
}
